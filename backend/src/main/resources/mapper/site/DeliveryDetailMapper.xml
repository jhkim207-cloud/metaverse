<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.biz.management.site.mapper.DeliveryDetailMapper">

    <select id="findByDeliveryNo" resultType="com.biz.management.site.dto.DeliveryDetailDto">
        SELECT id,
               delivery_no,
               line_no,
               order_no,
               order_line_no,
               material_cd,
               material_nm,
               category,
               thickness,
               width,
               height,
               order_quantity,
               unshipped_quantity,
               delivery_qty,
               unit,
               area,
               unit_price,
               amount,
               tax,
               total_amount,
               remarks
          FROM hkgn.delivery_detail
         WHERE delivery_no = #{deliveryNo}
         ORDER BY line_no
    </select>

</mapper>
