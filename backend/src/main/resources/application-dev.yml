# 개발 서버 환경 설정

spring:
  datasource:
    url: jdbc:postgresql://168.107.43.244:5432/apps?currentSchema=hkgn
    username: postgres
    password: TNTdys1234
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 5000
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false

logging:
  level:
    root: INFO
    com.biz.management: DEBUG
