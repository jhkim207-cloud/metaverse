# 운영 환경 설정

spring:
  datasource:
    url: jdbc:postgresql://168.107.43.244:5432/postgres
    username: postgres
    password: TNTdys1234
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 10000
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      max-lifetime: 600000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: false

logging:
  level:
    root: WARN
    com.biz.management: INFO
